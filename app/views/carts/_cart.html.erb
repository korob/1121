<h2>Корзина</h2>
<table>
	<%= render(cart.line_items) %>
	<tr class="total_line">
		<td colspan="2">Итого</td>
		<td class="total_cell"><%= cart.total_price %> руб.</td>
	</tr>
</table>
<%= button_to 'Оформить', new_order_path, method: :get %>
<%= button_to 'Очистить Корзину', cart, method: :delete %>