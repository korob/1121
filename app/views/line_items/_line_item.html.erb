
<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
<tr>
<% end %>
	<td><%= line_item.quantily %>&times</td>
	<td><%= line_item.product.title %></td>
	<td class="item_price"><%= line_item.total_price %> руб.</td>
</tr>